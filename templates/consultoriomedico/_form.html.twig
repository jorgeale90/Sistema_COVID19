{{ form_start(form) }}
<div class="card-body">
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                {% if(form_errors(form.nombre)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.nombre) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.nombre, 'CMF (*):') }}</label>
                {{ form_widget(form.nombre, { 'attr': { 'class': 'form-control' } }) }}
            </div>

            <div class="form-group">
                {% if(form_errors(form.municipio)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.municipio) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.municipio, 'Municipio (*):') }}</label>
                {{ form_widget(form.municipio, { 'attr': { 'class': 'form-control select2' } }) }}
            </div>

            <div class="form-group">
                {% if(form_errors(form.provincia)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.provincia) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.provincia, 'Provincia (*):') }}</label>
                {{ form_widget(form.provincia, { 'attr': { 'class': 'form-control select2' } }) }}
            </div>

            <div class="form-group">
                {% if(form_errors(form.areasalud)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.areasalud) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.areasalud, 'Area de Salud (*):') }}</label>
                {{ form_widget(form.areasalud, { 'attr': { 'class': 'form-control select2' } }) }}
            </div>
        </div>
    </div>
    <button class="btn btn-success"><i class="fa fa-plus"></i> {{ button_label|default('Añadir CMF') }}</button>
    <button class="btn btn-outline-info"><i class="icofont icofont-exchange"></i><a href="{{ path('consultoriomedico_index') }}">Volver al Listado</a></button>
</div>
{{ form_end(form) }}