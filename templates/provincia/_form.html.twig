{{ form_start(form) }}
<div class="card-body">
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                {% if(form_errors(form.nombre)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.nombre) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.nombre, 'Provincia (*):') }}</label>
                {{ form_widget(form.nombre, { 'attr': { 'class': 'form-control' } }) }}
            </div>

            <div class="form-group">
                {% if(form_errors(form.pais)) %}
                    <div class="alert alert-danger">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>{{ form_errors(form.pais) }}</strong>
                    </div>
                {% endif %}
                <label>{{ form_label(form.pais, 'País (*):') }}</label>
                {{ form_widget(form.pais, { 'attr': { 'class': 'form-control select2' } }) }}
            </div>
        </div>
    </div>
    <button class="btn btn-success"><i class="fa fa-plus"></i> {{ button_label|default('Añadir Provincia') }}</button>
    <button class="btn btn-outline-info"><i class="icofont icofont-exchange"></i><a href="{{ path('provincia_index') }}">Volver al Listado</a></button>
</div>
{{ form_end(form) }}